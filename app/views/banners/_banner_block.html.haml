-# = link_to campaign, :class=>'campaign_link' do 

.b-full-banner
  = link_to banner, :class=>'b-banner' do
    .banner_image= image_tag( banner.banner.url(:cropped) )
    .banner_info
      .banner_name
        = banner.name
      .banner_format= banner.labeled_format
      .banner_details
        - c = banner.campaigns.live
        - if c.present?
          - if defined?(campaign) and c.count == 1 and c.first == campaign
            Участвует только в этой кампании
          - else
            Участвует в кампаниях:
            = c.count
        - else
          В каманиях не участвует
  .banner_states
    = state_actions(defined?(campaign) ? campaign : banner)
  .clear
-#       %tr
-#         %td.banner_image
-#           = link_to image_tag( banner.banner.url(:mini) ), banner
-#         %td.banner_info
-#           = link_to banner do
-#             .1_line= banner.name
-#             .format
-#               = banner.format
-#               = "flash" if banner.is_flash?
-#             .campaigns_count
-#               В кампаниях:
-#               = banner.campaigns.live.count

-#         %td= banner.state

-# %br

