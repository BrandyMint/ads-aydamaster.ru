

%h2= "Площадка &#8470;#{@place.uid} '#{@website}/#{@place.name}'".html_safe

%p= "Типоразмер: #{@place.format}"

%p
  %b State:
  = @place.state

%p
  = state_actions(@place)

= render :partial => "shared/activity_log", :locals => {:activities => @place.activity_log_entries}

= link_to '&larr; список площадок'.html_safe, websites_path
